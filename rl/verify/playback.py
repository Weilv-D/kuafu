"""Native interactive playback is disabled until it shares the release evaluator path."""

if __name__ == "__main__":
    raise SystemExit("Use rl/verify/eval_policy.py for schema-compatible native evaluation.")
