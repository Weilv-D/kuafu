# KUAFU RL 依赖 — 精确锁定版本（保证可复现训练）
# 安装: rl/.venv/bin/pip install -r rl/requirements.txt

# ---- 验证必需（CPU 即可，物理验证 + viewer）----
mujoco==3.10.0
numpy==2.5.1
scipy==1.18.0

# ---- 训练必需（GPU + MJX + PyTorch 混合栈）----
# JAX cuda13 与 PyTorch 共享 CUDA 13 runtime, DLPack 零拷贝桥接
jax==0.10.2
jaxlib==0.10.2
jax-cuda13-plugin==0.10.2
jax-cuda13-pjrt==0.10.2
mujoco-mjx==3.10.0
flax==0.12.7
ml_collections
mujoco-playground>=0.0.3
rsl-rl-lib==2.3.3
torch==2.12.1

# ---- 导出 + 日志 ----
onnxruntime==1.27.0
tensorboard==2.21.0
wandb==0.28.0

# ---- 遥控(teleop) ----
pygame==2.6.1
